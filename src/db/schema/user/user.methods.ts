import { resolve } from 'path';
import { IUserDocument, IUserModel } from './user.types';

/**
 *
 * @param {IUserModel} this
 * @param {number} idx
 */
export async function setPasswordQuest(this: IUserDocument, idx: number): Promise<void> {
    resolve();
}
